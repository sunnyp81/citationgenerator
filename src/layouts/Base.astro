---
import '../styles/global.css';
import { GA_MEASUREMENT_ID, AFFILIATE_LIVE } from '../config/site.js';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  jsonld?: object[];
  noindex?: boolean;
}

const { title, description, canonical, jsonld = [], noindex = false } = Astro.props;
const canonicalUrl = canonical ?? new URL(Astro.url.pathname, Astro.site).href;
const ogImage = new URL('/og-default.png', Astro.site).href;
const year = 2026; // build-time constant; never rendered into slugs

// Sitewide Organization entity for E-E-A-T. Built on the open CSL standard; no fabricated author.
const orgSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'Citation Generator',
  url: 'https://citationgenerator.store',
  logo: 'https://citationgenerator.store/favicon.svg',
  description: 'Free citation generator that formats accurate references in every major style using the open Citation Style Language standard.',
  knowsAbout: ['citation styles', 'academic referencing', 'bibliographies', 'Citation Style Language'],
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex, follow" />}
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage} />
    <script type="application/ld+json" set:html={JSON.stringify(orgSchema)} />
    {jsonld.map((block) => (
      <script type="application/ld+json" set:html={JSON.stringify(block)} />
    ))}
    {GA_MEASUREMENT_ID && (
      <Fragment>
        <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${GA_MEASUREMENT_ID}`}></script>
        <script is:inline set:html={`window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag('js',new Date());gtag('config','${GA_MEASUREMENT_ID}');`}></script>
      </Fragment>
    )}
  </head>
  <body class="min-h-screen flex flex-col">
    <header class="border-b border-[var(--color-line)] bg-white sticky top-0 z-40">
      <div class="mx-auto max-w-5xl px-4 h-14 flex items-center justify-between">
        <a href="/" class="font-bold text-lg text-[var(--color-ink)]">
          citation<span class="text-[var(--color-brand)]">generator</span>
        </a>
        <nav class="flex gap-5 text-sm font-medium text-[var(--color-muted)]">
          <a href="/citation-generators" class="hover:text-[var(--color-brand)]">Styles</a>
          <a href="/how-to-cite" class="hover:text-[var(--color-brand)]">How to cite</a>
          <a href="/tools" class="hover:text-[var(--color-brand)]">Tools</a>
        </nav>
      </div>
    </header>

    <main class="flex-1">
      <slot />
    </main>

    <footer class="border-t border-[var(--color-line)] bg-[var(--color-bg-soft)] mt-16">
      <div class="mx-auto max-w-5xl px-4 py-10 text-sm text-[var(--color-muted)]">
        <div class="flex flex-wrap gap-x-8 gap-y-2 mb-6">
          <a href="/citation-generators" class="hover:text-[var(--color-brand)]">All citation styles</a>
          <a href="/how-to-cite" class="hover:text-[var(--color-brand)]">Citation guides</a>
          <a href="/tools" class="hover:text-[var(--color-brand)]">Free tools</a>
          <a href="/about" class="hover:text-[var(--color-brand)]">How citations are generated</a>
          <a href="/all-pages" class="hover:text-[var(--color-brand)]">All pages</a>
        </div>
        <p>Free citation generator. Accurate references in every major style, built on open Citation Style Language standards. No sign up.</p>
        {AFFILIATE_LIVE && <p class="mt-2">Some links on this site are affiliate links. If you buy through them we may earn a commission, at no extra cost to you. This never affects which tools we recommend.</p>}
        <p class="mt-2">&copy; {year} citationgenerator.store</p>
      </div>
    </footer>

    <!-- Outbound partner-click tracking: fires a GA4 event so we can measure demand for
         Grammarly/Quetext before committing to affiliate links. No-op until GA id is set. -->
    <script is:inline>
      document.addEventListener('click', (e) => {
        const a = e.target.closest && e.target.closest('[data-aff]');
        if (!a || typeof gtag !== 'function') return;
        gtag('event', 'affiliate_click', {
          partner: a.dataset.aff,
          placement: a.dataset.affPlacement || '',
          page_path: location.pathname,
          transport_type: 'beacon',
        });
      });
    </script>
  </body>
</html>
